/*funcion para cambiar el texto de los botones de On a Off*/

function turnOff(element) {
    // innerText es el que permite realizar el cambio de texto.
    element.innerText = "Off";
}

/*funcion para remover elementos al hacer click sobre ellos*/
function hide(element) {
    //remove es el que permite esconder los elementos
    element.remove();
}
